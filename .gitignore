node_modules
.next
out
dist
coverage
playwright-report
test-results
.turbo
.vercel
*.log
.DS_Store
.env
.env.local
.env.*.local
/data
*.tsbuildinfo
# Next.js regenerates next-env.d.ts per run with the dist dir of that run
# (./.next/dev/types in dev, ./.next/types in build). Tracking it means any
# check run that starts the dev server leaves the tree dirty.
next-env.d.ts
logs/**
.mypy_cache
.pytest_cache
.ruff_cache
.venv
